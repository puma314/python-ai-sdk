[python/packages/provider/language_model/v2/language_model_v2_finish_reason.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/language_model/v2/language_model_v2_data_content.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/language_model/v2/language_model_v2_data_content.py] Type alias uses bare assignment `LanguageModelV2DataContent = bytes | str` rather than `TypeAlias` annotation or PEP 695 `type` statement. All three forms are valid; this is a style preference, not a correctness issue.
[python/packages/provider/json_value/json_value.py] Module docstring placed after `from __future__ import annotations` (line 3); PEP 257 recommends module docstring as the first statement. Non-blocking style preference.
[python/packages/provider/json_value/json_value.py] `JSONObject = dict[str, JSONValue | None]` — the `| None` is redundant since `None` is already a member of `JSONValue`. Semantically harmless. Non-blocking.
[python/packages/provider/json_value/json_value.py] Inline comment on lines 13–14 duplicates the module docstring content; minor redundancy. Non-blocking.
[python/packages/provider/language_model/v2/language_model_v2_provider_defined_tool.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/language_model/v2/language_model_v2_provider_defined_tool.py] `args: dict[str, Any] = field(default_factory=dict)` gives `args` a default of `{}`, but the TypeScript source has `args: Record<string, unknown>` as a required field (no default). This makes Python construction more permissive than TS. Semantically harmless since `{}` is a valid value, but it's a subtle API difference. Non-blocking.
[python/packages/provider/language_model/v2/language_model_v2_response_metadata.py] Unused import: 'NotRequired' is imported from typing but never used (translator chose total=False instead of per-field NotRequired annotations)
[python/packages/provider/language_model/v2/language_model_v2_tool_choice.py] `tool_name: str = ""` gives a default of empty string, but the TypeScript source has `toolName: string` as a required field. The default is a necessary consequence of Python dataclass ordering rules (type field has a default, so subsequent fields must too). Semantically harmless. Non-blocking.
[python/packages/provider/language_model/v3/language_model_v3_data_content.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/language_model/v3/language_model_v3_data_content.py] TypeScript `URL` is collapsed into `str` (i.e. `bytes | str` covers `Uint8Array | string | URL`). Reasonable pragmatic choice since Python has no standard URL type; documented in an inline comment. Non-blocking design note.
[python/packages/provider/language_model/v3/language_model_v3_response_metadata.py] Module docstring is placed after 'from __future__ import annotations' rather than before it. PEP 257 recommends the docstring be the first statement; consider moving it above the future import.

[python/packages/provider/language_model/v2/language_model_v2_tool_choice.py] Module docstring is placed after `from __future__ import annotations`; convention prefers the docstring first, but this is syntactically valid Python.
[python/packages/provider/language_model/v2/language_model_v2_tool_choice.py] `SpecificToolChoice.tool_name` has a default of `""` (required by Python dataclass ordering since `type` carries a default). A stricter alternative: place `tool_name` before `type` so it is a required positional field. Current approach is a common/acceptable trade-off.
[python/packages/provider/language_model/v3/language_model_v3_provider_tool.py] Module docstring placed after `from __future__ import annotations` (line 2) — Python will not populate __doc__; the docstring should come first, with `from __future__ import annotations` immediately after.
[python/packages/provider/language_model/v3/language_model_v3_response_metadata.py] Module docstring placed after 'from __future__ import annotations' — Python will not register it as module.__doc__. Consider moving the docstring before the future import, or accept as cosmetic.
[python/packages/provider/language_model/v2/language_model_v2_usage.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/shared/v2/shared_v2_headers.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/shared/v3/shared_v3_warning.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement. Non-blocking style preference.
[python/packages/provider/shared/v3/shared_v3_warning.py] `feature: str = ''` and `message: str = ''` give defaults to fields that are required in the TypeScript source. This is a necessary Python dataclass concession (the `type` discriminator field carries a default, so all subsequent fields must also have defaults). Non-blocking.
[python/packages/provider/embedding_model/v3/embedding_model_v3_result.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file. Non-blocking style preference.
[python/packages/provider/embedding_model/v3/embedding_model_v3_result.py] `embeddings: list[EmbeddingModelV3Embedding]` and `warnings: list[SharedV3Warning]` are required fields in the TypeScript source (no `?`) but are given `field(default_factory=list)` defaults in Python. A stricter translation would make them required positional fields by placing them before any optional fields. Current approach is a common dataclass trade-off. Non-blocking.
[python/packages/provider/embedding_model/v2/embedding_model_v2.py] Module docstring appears after `from __future__ import annotations` rather than before it; PEP 257 recommends the module docstring be the first statement in the file, so `module.__doc__` will not be populated. Non-blocking style preference.
[python/packages/provider/shared/v2/shared_v2_headers.py] Module docstring appears after 'from __future__ import annotations' (line 2). PEP 257 recommends the module docstring be the first statement in the file. Convention in this codebase may already accept this ordering since 'from __future__ import annotations' must appear first per PEP 236.
[python/packages/provider/shared/v3/shared_v3_warning.py] Required fields `feature` (on UnsupportedWarning and CompatibilityWarning) and `message` (on OtherWarning) are given default values of `''`, making them silently optional at construction time (e.g. `UnsupportedWarning()` is valid Python but would be an invalid object). In the TypeScript source these were non-optional fields. Consider removing the defaults or adding a `__post_init__` guard, though this is a design trade-off rather than a functional breakage.
[python/packages/provider/language_model/v3/language_model_v3_tool_choice.py] Module docstring appears after `from __future__ import annotations` (line 2). PEP 257 recommends the module docstring be the very first statement in the file; `from __future__ import annotations` should technically follow it.
[python/packages/provider/language_model/v3/language_model_v3_tool_choice.py] LanguageModelV3ToolChoiceTool.tool_name is given a default of "" to satisfy Python dataclass field-ordering rules (a field with no default cannot follow a field that has one). This means LanguageModelV3ToolChoiceTool() can be instantiated with an empty tool_name, which is semantically invalid per the source (toolName was required in TS). Consider reordering so tool_name comes before type, or using a sentinel/post-init validator.
[python/packages/provider/embedding_model/v3/embedding_model_v3_result.py] Module docstring appears after `from __future__ import annotations` (line 1); PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/embedding_model/v3/embedding_model_v3_result.py] `embeddings` and `warnings` were required (non-optional) fields in the TypeScript source but are given `field(default_factory=list)` defaults in the Python dataclass, semantically loosening the contract.
[python/packages/provider/embedding_model/v3/embedding_model_v3_result.py] `EmbeddingModelV3ResultUsage.tokens` was a required field in the TypeScript inline type `{ tokens: number }` but has a default of `0` in Python, making it optional at construction time.
[python/packages/provider/embedding_model/v2/embedding_model_v2.py] Unused import: `Generic` is imported from `typing` but never referenced. `Protocol[VALUE]` does not require a separate `Generic` import; ruff will flag this as F401.
[python/packages/provider/embedding_model/v2/embedding_model_v2.py] Module docstring appears after `from __future__ import annotations` (line 2). PEP 257 recommends the module docstring be the very first statement in the file. Translation rules do not mandate a specific order, but some linters may flag it.
[python/packages/provider/errors/invalid_prompt_error.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the module docstring be the first statement in the file. The translation rules mandate 'from __future__ import annotations' at the top, creating a conflict — this is a known project-level ambiguity worth resolving consistently across all files.
[python/packages/provider/errors/empty_response_body_error.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). Because `from __future__ import annotations` is the first statement, the triple-quoted string that follows is treated as a floating expression—not the module's `__doc__` attribute. PEP 257 recommends the module docstring be the very first statement, with `from __future__` imports coming immediately after it.
[python/packages/provider/errors/empty_response_body_error.py] is_instance is declared as @classmethod (receives cls) but never uses cls—it calls AISDKError.has_marker with the module-level _MARKER constant directly, matching the TS source exactly. A @staticmethod would be slightly more precise, but this is harmless.
[python/packages/provider/errors/api_call_error.py] Module docstring appears on line 3, after 'from __future__ import annotations' on line 1. PEP 257 specifies the docstring should be the first statement of the module. The standard workaround is to place the docstring first, then the __future__ import — or to accept this ordering as a project convention since many formatters (e.g. isort, ruff) also place __future__ imports first.
[python/packages/provider/errors/load_setting_error.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 says the module docstring should be the first statement, but 'from __future__' imports are required to precede all other statements — so this placement is technically the only valid option and is widely accepted. No action needed.
[python/packages/provider/errors/no_content_generated_error.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the module docstring be the first statement in the file. Some linters (e.g. ruff D400/D401) may flag this ordering.
[python/packages/provider/errors/load_api_key_error.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 recommends the module docstring be the first statement in the file. The __future__ import must come first per Python spec, so this placement is a known trade-off, but some style guides resolve it by putting the docstring first and the __future__ import second (which requires a # noqa or ruff ignore), or by accepting the current order. Not a runtime issue.
[python/packages/provider/errors/json_parse_error.py] Module docstring appears after `from __future__ import annotations` (lines 1 and 3–6); PEP 257 recommends the module docstring be the first statement in the module. Translation rules also say `from __future__ import annotations` must be at the top, creating a conflict — current placement is a style choice either way.
[python/packages/provider/errors/json_parse_error.py] TS declares `readonly text: string`; translation rules recommend `Final[T]` for individual readonly fields on a mutable class, but the Python uses a plain `text: str` class-level annotation without `Final`. No runtime impact, but the immutability contract is not communicated to type checkers.
[python/packages/provider/errors/json_parse_error.py] TS constructor signature makes `cause` required (`{ text: string; cause: unknown }`), but the Python translation makes it optional (`cause: BaseException | None = None`). Functionally safe, but callers that relied on the required parameter won't get a type error if they omit it.
[python/packages/provider/errors/no_such_model_error.py] Module docstring appears after `from __future__ import annotations` (line 1) rather than as the very first statement. This is technically the only valid ordering given PEP 236 constraints, so no action needed — purely informational.
[python/packages/provider/errors/no_such_model_error.py] A named `ModelType` type alias was introduced (not present inline in the TS source). This improves readability and is harmless, but is a translator addition beyond strict 1-to-1 mapping.
[python/packages/provider/errors/too_many_embedding_values_for_call_error.py] Module docstring appears after 'from __future__ import annotations' (line 3) rather than as the very first statement of the module. PEP 257 recommends the docstring precede all imports, but placing it after the __future__ import is a widely accepted convention. No action required unless the project enforces strict PEP 257 ordering.
[python/packages/provider/errors/unsupported_functionality_error.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 recommends the module docstring be the first statement in the file. While `from __future__` imports are technically permitted before a module docstring in CPython, the conventional order is docstring first, then `from __future__` imports. Functionally harmless.
[python/packages/provider/shared/v2/shared_v2_provider_metadata.py] TS source uses a relative import ('../../json-value/json-value'); translation uses an absolute import ('from ai_sdk.provider.json_value.json_value import JSONValue') instead of the equivalent relative form ('from ...json_value.json_value import JSONValue'). The translation rules specify relative imports should stay relative within the same package. The absolute form is functionally correct if the package is installed, but inconsistent with the convention.
[python/packages/provider/shared/v2/shared_v2_provider_metadata.py] Module docstring is placed after 'from __future__ import annotations' (lines 3-25). PEP 257 prefers the module docstring as the first statement, but future imports must legally precede it — this placement is the only valid option and is functionally correct; some linters may flag it.
[python/packages/provider/errors/type_validation_error.py] Module docstring appears after `from __future__ import annotations` (lines 1 then 3-6). PEP 257 recommends the module docstring be the very first statement in the file; `from __future__` imports are the one accepted exception but style guides differ on ordering.
[python/packages/provider/errors/type_validation_error.py] TypeScript `cause.value === value` is translated as `cause.value is value` (identity check) rather than `cause.value == value` (equality). For object types this is semantically equivalent, but for primitive `value` types (e.g. a plain string or int) Python identity may not hold even when values are equal. Low practical risk given the SDK context (values are typically complex objects), but worth noting.
[python/packages/provider/errors/type_validation_error.py] Field docstrings on `TypeValidationContext` are placed as inline string literals immediately after each field definition (lines 24, 27, 30). Google style would collect these under an `Attributes:` section in the class-level docstring rather than as per-field literals.
[python/packages/provider/json_value/is_json.py] Module docstring placement: `from __future__ import annotations` appears on line 1, before the module docstring on lines 3–6. Per PEP 257 the module docstring should be the first statement; per PEP 236, `__future__` imports should follow the docstring but precede all other imports. Consider swapping the order.
[python/packages/provider/json_value/is_json.py] Behavioral deviation in `is_json_object`: TypeScript's `typeof value === 'object'` matches arrays (JS arrays are objects), so `isJSONObject([1, 'hello', true])` returns `true` in the original. Python's `isinstance(value, dict)` correctly excludes lists, making `is_json_object([1, 'hello', true])` return `False`. The Python behaviour is semantically more correct and consistent with the `JSONObject` type definition (which is `dict[str, JSONValue | None]`), but it is a minor runtime divergence from the TypeScript source worth noting.
[python/packages/provider/shared/v3/shared_v3_provider_metadata.py] Docstring uses RST-style 'Example::' (double colon) for the code block instead of Google-style 'Example:' (single colon) as required by the translation rules.
[python/packages/provider/shared/v3/shared_v3_provider_options.py] Example block uses RST-style double-colon syntax (`Example::`) rather than Google-style single-colon (`Example:`). Minor docstring style inconsistency.
[python/packages/provider/speech_model/v2/speech_model_v2_call_options.py] Unused import: `from typing import Any` — `Any` is never referenced in the file. This will trigger a ruff F401 lint warning.
[python/packages/provider/speech_model/v2/speech_model_v2_call_options.py] Unused import: `field` is imported from `dataclasses` but never used (no `field(...)` calls in the dataclass). This will trigger a ruff F401 lint warning.
[python/packages/provider/speech_model/v2/speech_model_v2_call_options.py] Module docstring appears after `from __future__ import annotations` (line 2 vs line 1). PEP 257 technically recommends the module docstring be the first statement, but since PEP 236 mandates `from __future__` imports come before anything else, this ordering is forced and acceptable — some linters may still flag it.
[python/packages/provider/shared/v2/shared_v2_provider_options.py] The JSDoc block comment in TypeScript was attached to the `SharedV2ProviderOptions` type alias, not the module. In Python it was promoted to the module docstring. This is the correct approach since Python type aliases cannot carry docstrings, but reviewers should note the semantic scope changed from type-level to module-level documentation.
[python/packages/provider/shared/v2/shared_v2_provider_options.py] Module docstring is placed after `from __future__ import annotations` (line 1 → docstring starts line 3). This matches the translation rules' own example pattern, but PEP 257 technically considers the docstring as needing to be the first string literal. Since `from __future__` must come first, this is an unavoidable trade-off and not a real issue.
[python/packages/provider/json_value/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3); PEP 257 recommends the docstring be the very first statement of the module, before any imports including __future__.
[python/packages/provider/errors/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 2 vs line 1). PEP 257 recommends the module docstring be the first statement in the file. The future import should follow the docstring, not precede it.
[python/packages/provider/json_value/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the docstring be the first statement, but PEP 236 requires future imports to precede everything else — this placement is the accepted convention and not a real issue.
[python/packages/provider/errors/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the docstring be the first statement in a module. However, placing 'from __future__ import annotations' first is a widely accepted convention and many linters (including ruff) accept this ordering, so this is a very minor style point.
[python/packages/provider/language_model/v2/language_model_v2_source.py] Module docstring appears after 'from __future__ import annotations' (line 3 vs line 1). PEP 257 recommends the module docstring be the first statement in the file.
[python/packages/provider/language_model/v2/language_model_v2_source.py] Required TypeScript fields (id, url on UrlSource; id, mediaType, title on DocumentSource) are translated with empty-string defaults (= '') in the frozen dataclasses. This weakens the construction-time contract — callers can instantiate these with no arguments — though it is a common dataclass trade-off when all fields need defaults for ordering reasons.
[python/packages/provider/transcription_model/v2/transcription_model_v2_call_options.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 requires the module docstring to be the first statement; 'from __future__' imports are allowed immediately after a docstring. As currently written, the triple-quoted string is not assigned to __doc__ and is silently discarded. Recommended order: module docstring first, then 'from __future__ import annotations'.
[python/packages/provider/language_model/v3/language_model_v3_tool_call.py] Module docstring appears after `from __future__ import annotations` (lines 1-5). PEP 257 recommends the module docstring be the very first statement in the module.
[python/packages/provider/language_model/v3/language_model_v3_tool_call.py] `field` is imported from `dataclasses` but is never used in the file — ruff would flag this as F401 (unused import).
[python/packages/provider/language_model/v2/language_model_v2_tool_result.py] field is imported from dataclasses but never used; would produce an unused-import linting warning under ruff/pyright.
[python/packages/provider/language_model/v2/language_model_v2_tool_result.py] Module docstring appears after 'from __future__ import annotations' (line 1); PEP 257 recommends the docstring be the first statement in the module.
[python/packages/provider/language_model/v2/language_model_v2_tool_result.py] The TypeScript-required fields tool_call_id, tool_name, and result are given empty-string / None defaults due to Python dataclass ordering constraints (the type discriminant field with a default forces all subsequent fields to also carry defaults). This weakens the constructor contract relative to the original TypeScript where these three fields were mandatory.
[python/packages/provider/language_model/v2/language_model_v2_text.py] Unused import: `field` is imported from `dataclasses` but never used in the file. `ruff check` would flag this as F401.
[python/packages/provider/language_model/v2/language_model_v2_text.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 lists the module docstring as the first statement, but `from __future__` is syntactically required to precede all other statements. Style guides differ on ordering here; this is a known tension.
[python/packages/provider/language_model/v2/language_model_v2_text.py] `text: str = ''` gives the field a default empty string, making it optional at construction time. In the TypeScript source `text: string` is a required field. The default was necessary to satisfy Python dataclass rules (a field without a default cannot follow a field with a default, i.e. `type: Literal['text'] = 'text'`). The semantic difference is minor but worth noting.
[python/packages/provider/language_model/v3/language_model_v3_tool_approval_request.py] Unused import: `field` is imported from `dataclasses` but never used in the file (no `field(...)` calls are present). Ruff will flag this as F401.
[python/packages/provider/language_model/v3/language_model_v3_tool_approval_request.py] Module docstring appears after `from __future__ import annotations` rather than as the very first statement. PEP 257 recommends the module docstring precede all imports, but this is a project-wide pattern in the translation.
[python/packages/provider/language_model/v3/language_model_v3_tool_approval_request.py] `approval_id: str = ''` and `tool_call_id: str = ''` assign empty-string defaults to fields that are required (non-optional) in the TypeScript source. The empty defaults are a consequence of `type` carrying a default value and appearing first (dataclass ordering rules require all following fields to also have defaults). Callers can inadvertently construct a structurally invalid instance with empty IDs; this may warrant a note in the class docstring or a `__post_init__` validator.
[python/packages/provider/language_model/v2/language_model_v2_tool_call.py] The `field` name is imported from `dataclasses` (`from dataclasses import dataclass, field`) but never used in the file. This will trigger a linting warning (ruff F401 / flake8 unused-import).
[python/packages/provider/language_model/v2/language_model_v2_tool_call.py] Required TypeScript fields `toolCallId`, `toolName`, and `input` are translated with empty-string defaults (`= ''`). This is a forced compromise because the `type` discriminator field needs a default value and Python dataclasses require all subsequent fields to also have defaults. As a result, `LanguageModelV2ToolCall()` can be constructed without supplying any of these logically-required fields. Consider using `__post_init__` validation or a Pydantic model if stricter construction-time enforcement is desired.
[python/packages/provider/language_model/v2/language_model_v2_tool_call.py] Module docstring is placed after `from __future__ import annotations`. PEP 257 recommends the module docstring as the first statement in the file, but `from __future__` imports must legally precede it — this is a common accepted pattern and not an error.
[python/packages/provider/language_model/v3/language_model_v3_source.py] Module docstring appears after 'from __future__ import annotations' (line 3) rather than as the very first statement per PEP 257. This is a known conflict between the project rule mandating 'from __future__ import annotations' at the top of every file and PEP 257 module docstring placement.
[python/packages/provider/language_model/v3/language_model_v3_source.py] Required fields in the TypeScript source ('id', 'url' on LanguageModelV3UrlSource; 'id', 'media_type', 'title' on LanguageModelV3DocumentSource) have been given empty-string defaults (''), making them silently optional at construction time. The TypeScript originals required these fields. Consider reordering so required fields lack defaults and discriminator fields ('type', 'source_type') appear after them with their literal defaults.
[python/packages/provider/language_model/v3/language_model_v3_source.py] LanguageModelV3UrlSource and LanguageModelV3DocumentSource are publicly accessible module-level names, but only LanguageModelV3Source was exported in the TypeScript source. This is unavoidable in Python (Union members must be named), but consumers may depend on the internal names unintentionally.
[python/packages/provider/embedding_model/v2/__init__.py] Module docstring appears on line 2, after 'from __future__ import annotations' on line 1. PEP 257 recommends the docstring be the first statement in a module, but PEP 236 mandates __future__ imports come before everything else. This is a known trade-off; some teams resolve it by omitting the module docstring and relying on the 'Translated from:' comment only. Not a blocker.
[python/packages/provider/transcription_model/v3/transcription_model_v3_call_options.py] Module docstring is placed after `from __future__ import annotations` (lines 1–6). PEP 257 recommends the docstring be the very first statement of the module, with `from __future__` following it (PEP 236 permits `__future__` imports after a docstring). The current ordering is a common pattern in this codebase and is functionally harmless, but is non-standard.
[python/packages/provider/transcription_model/v3/transcription_model_v3_call_options.py] `import asyncio` is brought in as a module reference solely for the `asyncio.Event` type annotation. Because `from __future__ import annotations` is active, the annotation is a lazy string and the import is not strictly required at runtime for type-checking purposes. Minor style preference: `from asyncio import Event` would be slightly clearer.
[python/packages/provider/language_model/v3/language_model_v3_prompt.py] Module docstring appears after `from __future__ import annotations` (line 1). PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/language_model/v3/language_model_v3_prompt.py] LanguageModelV3ToolResultPart.output is required (non-optional) in the TypeScript source but is translated as `LanguageModelV3ToolResultOutput | None = None` in Python. This relaxes the type constraint, though it is a practical workaround for frozen dataclass field-ordering requirements.
[python/packages/provider/language_model/v3/language_model_v3_prompt.py] LanguageModelV3ToolCallPart.input is typed as `object = None` (with a `# type: ignore[assignment]` suppression) rather than `Any = None`. The translation rules map TypeScript `unknown` to `Any`; `object` is less idiomatic here.
[python/packages/provider/language_model/v3/language_model_v3_prompt.py] LanguageModelV3FilePart.data is a required field in the TypeScript source but is given a default of `''` in Python to satisfy frozen dataclass field-ordering constraints. This same pattern applies broadly across the file (e.g., `tool_call_id`, `tool_name`, etc.), all of which are required in TypeScript but receive empty-string defaults.
[python/packages/provider/language_model/v2/language_model_v2_prompt.py] Absolute imports used where translation rules §8 specify relative imports. TypeScript source uses relative paths (e.g. '../../json-value/json-value', './language-model-v2-data-content') which should map to relative Python imports (e.g. from ...json_value.json_value import JSONValue, from .language_model_v2_data_content import LanguageModelV2DataContent). The absolute ai_sdk.provider.* imports work in a properly installed package environment but deviate from the stated convention.
[python/packages/provider/language_model/v2/language_model_v2_prompt.py] Module docstring appears after 'from __future__ import annotations' (lines 3-14). PEP 257 recommends the module docstring be the first statement, but Python requires __future__ imports to precede all other code. The current ordering is unavoidable given both requirements; worth noting for consistency across the codebase.
[python/packages/provider/language_model/v2/language_model_v2_prompt.py] Required fields in the source (e.g. 'output: LanguageModelV2ToolResultOutput' in LanguageModelV2ToolResultPart, 'text: str' in LanguageModelV2TextPart and LanguageModelV2ReasoningPart, 'data' and 'mediaType' in LanguageModelV2FilePart) are given empty/None defaults. This was necessary because Python dataclasses require fields-with-defaults to follow fields-without-defaults, and the 'type' discriminant field (first, with a default) forces all subsequent fields to have defaults too. The downside is that required fields are no longer enforced at instantiation time.
[python/packages/provider/language_model/v3/language_model_v3_text.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 specifies the module docstring should be the first statement in the file; with this ordering Python will not set `module.__doc__` to this string. Preferred order: module docstring first, then `from __future__ import annotations`.
[python/packages/provider/language_model/v3/language_model_v3_text.py] `text: str = ''` gives a default empty-string value to what was a required field in TypeScript (`text: string`, no `?`). Necessary due to Python dataclass field-ordering constraints (the `type` field with its `Literal['text'] = 'text'` default must precede it), but worth noting as a semantic weakening of the constructor signature.
[python/packages/provider/shared/v2/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 2); PEP 257 recommends the module docstring be the first statement in the file. Consider placing the docstring before the __future__ import.
[python/packages/provider/language_model/v3/language_model_v3_usage.py] Module docstring appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the module docstring be the first statement in the file, with 'from __future__' imports following it.
[python/packages/provider/language_model/v3/language_model_v3_text.py] text: str = '' gives the field a default empty string, whereas the TypeScript source declares text: string as required (no '?'). This is a forced consequence of Python dataclass field-ordering rules (type appears first with a default, so subsequent fields must also have defaults). Consider using @dataclass(frozen=True, kw_only=True) with text: str (no default) to preserve the required-field semantics.
[python/packages/provider/language_model/v3/language_model_v3_text.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). CPython still assigns it to __doc__, but PEP 257 conventionally places the module docstring before any imports, including future imports.
[python/packages/provider/transcription_model/v2/transcription_model_v2_call_warning.py] Module docstring is placed after `from __future__ import annotations` (lines 3-6). Python introspection tools may not recognise it as the module docstring since it is not the first statement. Many codebases accept this placement because `from __future__` must come first per PEP 236, but it is worth standardising across translated files.
[python/packages/provider/transcription_model/v2/transcription_model_v2_call_warning.py] The `setting` field in `TranscriptionModelV2CallWarningUnsupportedSetting` is required (no `?`) in the TypeScript source, but receives a default of `'audio'` in Python, making it optional at construction time. The default is a side-effect of Python dataclass ordering constraints (since `type` has a default and precedes `setting`). Similarly `message` in `TranscriptionModelV2CallWarningOther` defaults to `''`. These defaults allow constructing structurally incomplete instances that would be rejected by TypeScript's type system.
[python/packages/provider/language_model/v3/language_model_v3_tool_result.py] result field is typed as JSONValue instead of NonNullable<JSONValue>. Since JSONValue in Python includes None (Union[None, str, int, float, bool, JSONObject, JSONArray]), this allows None at the type level, whereas the TypeScript source uses NonNullable<JSONValue> to explicitly exclude null/undefined. Consider using a type alias like JSONValueNonNull = str | int | float | bool | JSONObject | JSONArray for greater fidelity.
[python/packages/provider/language_model/v3/language_model_v3_tool_result.py] The three logically-required TS fields (toolCallId, toolName, result) are given default values (empty string, empty string, empty dict) to satisfy Python dataclass field-ordering constraints. This changes their construction-time semantics from required to optional. A keyword-only dataclass or runtime __post_init__ guard could enforce that non-None values are supplied.
[python/packages/provider/language_model/v3/language_model_v3_tool_result.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the module docstring be the very first statement in the file. Moving it before the __future__ import would be strictly compliant.
[python/packages/provider/speech_model/v2/speech_model_v2_call_warning.py] Fields `setting` (default='text') and `message` (default='') are required in the TypeScript source but have defaults in Python. This is a forced limitation of Python dataclasses: because the discriminator `type` field must appear first with a default Literal value, all subsequent fields must also carry defaults. Looser construction-time enforcement is the result, but runtime correctness is unaffected.
[python/packages/provider/speech_model/v2/speech_model_v2_call_warning.py] Module docstring appears after `from __future__ import annotations` (line 1). PEP 236 explicitly allows the module docstring *before* future imports, so the preferred order is: module docstring → `from __future__ import annotations`. Functionally harmless in CPython since the string literal is still assigned to `__doc__`.
[python/packages/provider/shared/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 2), making it a bare string expression rather than the true PEP 257 module docstring. This is a known tension with the translation rule that requires 'from __future__ import annotations' as the first line of every file.
[python/packages/provider/reranking_model/v3/reranking_model_v3_call_options.py] Module docstring appears after 'from __future__ import annotations' on line 1 (docstring is on lines 3–6). PEP 257 recommends the docstring be the very first statement of a module. This is a consistent project-wide pattern (same in shared/v3/__init__.py and json_value/json_value.py), so not unique to this file, but worth tracking.
[python/packages/provider/reranking_model/v3/reranking_model_v3.py] Module docstring appears after 'from __future__ import annotations' (lines 3–6). PEP 257 recommends the module docstring be the very first statement, but Python's language spec requires __future__ imports to precede everything else, so this ordering is an accepted trade-off. No action required.
[python/packages/provider/speech_model/v3/speech_model_v3.py] Module docstring appears after `from __future__ import annotations` (line 3). PEP 257 recommends the module docstring be the first statement in the file. The translation rules also say to put `from __future__ import annotations` at the top of every file, so these two guidelines are in tension. The current ordering is a common convention but worth flagging.
[python/packages/provider/speech_model/v3/speech_model_v3.py] In the `SpeechModelV3` Protocol, `do_generate` is declared as a plain `def` returning `Awaitable[SpeechModelV3GenerateResult]` rather than `async def`. Both are valid ways to express an async Protocol method in Python, but `async def` is more idiomatic for Protocol definitions of coroutines and aligns better with `async def` usage throughout the codebase.
[python/packages/provider/language_model/v3/language_model_v3_reasoning.py] Unused import: `field` is imported from `dataclasses` but never used in the file. `ruff` would flag this as F401. Consider removing it.
[python/packages/provider/language_model/v3/language_model_v3_reasoning.py] Module docstring appears after `from __future__ import annotations` (lines 3–6). PEP 257 recommends the module docstring be the very first statement; however, the translation rules also mandate `from __future__ import annotations` at the top of every file, creating an inherent conflict. This ordering is consistent across other translated files and may be an accepted project convention.
[python/packages/provider/embedding_model/v3/embedding_model_v3_call_options.py] The `values` field is required (non-optional) in the TypeScript source (`values: Array<string>` with no `?`), but the Python translation gives it `field(default_factory=list)`, making it silently optional. A caller that forgets to supply `values` will get an empty-list embedding call rather than a TypeError. Consider removing the default so the dataclass enforces it as required.
[python/packages/provider/embedding_model/v3/embedding_model_v3_call_options.py] Module docstring (lines 2–5) appears after `from __future__ import annotations` (line 1). PEP 257 recommends the module docstring be the very first statement; `from __future__ import annotations` should follow it.
[python/packages/provider/video_model/v3/video_model_v3_file.py] Unused import: `field` is imported from `dataclasses` (line 8) but never used in the file — no `field(...)` calls appear anywhere. This will produce a ruff/flake8 unused-import warning.
[python/packages/provider/video_model/v3/video_model_v3_file.py] Module docstring appears after `from __future__ import annotations` (lines 3–6). PEP 257 recommends the module docstring be the first statement in the file; the `from __future__` import is conventionally the sole exception allowed before it, but some style guides still prefer the docstring first.
[python/packages/provider/transcription_model/v3/transcription_model_v3.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 recommends the module docstring be the very first statement in the file, before any imports including __future__ imports.
[python/packages/provider/speech_model/v2/speech_model_v2.py] specification_version property in the SpeechModelV2 Protocol returns `str` instead of `Literal['v2']`; the TypeScript source uses the literal type 'v2' as a discriminant, so `Literal['v2']` would be more precise and would help type-checkers narrow discriminated unions correctly.
[python/packages/provider/speech_model/v2/speech_model_v2.py] Module docstring appears after `from __future__ import annotations` (line 1) rather than as the first statement. This is a known unavoidable tension between PEP 236 (future imports must come first) and PEP 257 (docstring should be the first statement).
[python/packages/provider/speech_model/v2/speech_model_v2.py] Three helper dataclasses (SpeechModelV2RequestInfo, SpeechModelV2ResponseInfo, SpeechModelV2GenerateResult) are new named public exports not explicitly present in the TypeScript source, which used inline anonymous object types. This is a necessary and correct Python idiom, but the package __init__.py may need updating to re-export them if they are part of the public API.
[python/packages/provider/image_model/v3/image_model_v3_file.py] Module docstring appears after 'from __future__ import annotations'; PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/image_model/v3/image_model_v3_file.py] Required fields 'media_type', 'data', and 'url' receive default values ('' / b'') because the 'type' discriminator field is declared first with a default, which forces all subsequent dataclass fields to also carry defaults. An alternative is to declare 'type' with 'field(default="file", init=False)' so that the semantically-required fields remain mandatory at construction time.
[python/packages/provider/image_model/v3/image_model_v3_file.py] provider_options uses field(default=None) where a plain '= None' annotation would suffice for an immutable default in a frozen dataclass.
[python/packages/provider/transcription_model/v2/transcription_model_v2.py] Module docstring (lines 3-6) appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the module docstring be the very first statement in the file. The 'from __future__ import annotations' line should follow the docstring, not precede it.
[python/packages/provider/language_model/v2/language_model_v2_reasoning.py] 'field' is imported from 'dataclasses' but never used in the file; this would trigger an F401 unused-import warning under ruff check.
[python/packages/provider/language_model/v2/language_model_v2_reasoning.py] Module docstring appears after 'from __future__ import annotations' (line 2 vs line 1); PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/language_model/v2/language_model_v2_reasoning.py] 'text: str = ""' assigns a default value to a field that was required (non-optional) in the TypeScript source. This is a pragmatic workaround for Python dataclass field-ordering rules (a field with a default — 'type' — cannot precede a field without one), but it weakens the constructor contract by making 'text' optional at call sites.
[python/packages/provider/language_model/v3/language_model_v3_function_tool.py] Module docstring appears after `from __future__ import annotations` (lines 1–6). PEP 257 states the docstring should be the first statement of the module, but `from __future__` must syntactically precede it. The current ordering is consistent with common project convention and is not wrong in practice, but some linters may flag it.
[python/packages/provider/language_model/v3/language_model_v3_file.py] Module docstring (lines 3-9) appears after `from __future__ import annotations` (line 1). The string literal will not be set as `module.__doc__` because a non-docstring statement already precedes it. PEP 257 convention: place the module docstring first, then `from __future__ import annotations`.
[python/packages/provider/language_model/v3/language_model_v3_file.py] `field` is imported from `dataclasses` on line 11 but is never used anywhere in the file. This will trigger an F401 linting error under `ruff check`.
[python/packages/provider/language_model/v2/language_model_v2_function_tool.py] Module docstring appears after 'from __future__ import annotations'; PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/language_model/v2/language_model_v2_function_tool.py] Fields 'name' and 'input_schema' are required (non-optional) in the TypeScript source but have been given default values ('' and field(default_factory=dict)) in the Python dataclass due to field-ordering constraints caused by 'type' carrying a default. This slightly loosens the construction-time contract. An alternative would be to place the 'type' field last with its default, keeping 'name' and 'input_schema' required.
[python/packages/provider/image_model/v2/image_model_v2_call_options.py] Module docstring appears after `from __future__ import annotations` on line 1; PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/image_model/v2/image_model_v2_call_options.py] In TypeScript, `providerOptions` is a required field (no `?`), but the Python translation gives it `default_factory=dict` to satisfy dataclass field-ordering constraints (optional fields precede it). This is a pragmatic semantic loosening: callers can now omit it. Consider using a `TypedDict` or Pydantic `BaseModel` to preserve the required-field constraint without ordering issues.
[python/packages/provider/image_model/v2/image_model_v2_call_options.py] Template literal types `${number}x${number}` (size) and `${number}:${number}` (aspect_ratio) were translated to bare `str` with no runtime validators. Translation rules section 2 recommends adding a validator function and calling it in `__post_init__`. Note that TypeScript itself only enforces these statically, so omitting validators preserves runtime parity — but the rules call for them explicitly.
[python/packages/provider/language_model/v2/language_model_v2_content.py] Module docstring appears on line 2, after 'from __future__ import annotations' on line 1. PEP 257 recommends the module docstring be the first statement of the module. Consider moving the docstring above the future import, or accepting this as a consistent project-wide pattern.
[python/packages/provider/speech_model/v2/__init__.py] Module docstring appears on line 2, after 'from __future__ import annotations' (line 1). PEP 257 recommends the docstring be the first statement, though __future__ imports are a widely accepted exception. Consistent ordering across all translated files would be preferable.
[python/packages/provider/speech_model/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the docstring be the first statement, but __future__ imports are a language-mandated exception; some teams still prefer the docstring first with __future__ second. Minor and debatable.
[python/packages/provider/reranking_model/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' on line 1; PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/transcription_model/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' rather than as the first statement (PEP 257 recommends docstring first). However, this matches the project's own translation rules example, so it is intentional project convention.
[python/packages/provider/transcription_model/v2/__init__.py] Module docstring is placed after 'from __future__ import annotations' (line 2 vs line 1). PEP 257 recommends the module docstring be the very first statement. The project translation rules mandate 'from __future__ import annotations' at the top of every file, creating an inherent ordering conflict — worth resolving with a project-wide convention decision.
[python/packages/provider/language_model/v3/language_model_v3_content.py] Module docstring is placed after 'from __future__ import annotations' (lines 3-6). Per PEP 257 and PEP 236, the module docstring should be the first statement; 'from __future__' imports are explicitly allowed to follow the docstring. Recommended order: docstring → from __future__ import annotations → other imports.
[python/packages/provider/embedding_model/v3/embedding_model_v3.py] Module docstring appears on lines 3–7, after `from __future__ import annotations` on line 1. PEP 257 recommends the module docstring be the very first statement, but PEP 236 requires `from __future__` to come first — this is an inherent conflict with no perfect solution; current ordering is acceptable.
[python/packages/provider/embedding_model/v3/embedding_model_v3.py] @runtime_checkable was added to the Protocol class though the TypeScript source has no isInstance check pattern. Minor addition that doesn't hurt correctness.
[python/packages/provider/image_model/v3/image_model_v3_call_options.py] `from typing import Any` is present but `Any` is never used in the file — will trigger ruff F401 unused-import warning.
[python/packages/provider/image_model/v3/image_model_v3_call_options.py] Module docstring is placed after `from __future__ import annotations` rather than as the first statement; PEP 257 recommends the module docstring be the first statement in the file.
[python/packages/provider/image_model/v3/image_model_v3_call_options.py] `n: int = 1` gives a default value to a field that is required (no `?`) in the TypeScript source (`n: number`); this widens the Python API by making `n` optional.
[python/packages/provider/image_model/v3/image_model_v3_call_options.py] `provider_options: SharedV3ProviderOptions = field(default_factory=dict)` similarly adds a default to a required TypeScript field (`providerOptions: SharedV3ProviderOptions`).
[python/packages/provider/language_model/v3/language_model_v3_call_options.py] Module docstring appears on line 3, after `from __future__ import annotations` on line 1. PEP 257 recommends the module docstring be the very first statement in the file (before any imports, including `from __future__`).
[python/packages/provider/language_model/v3/language_model_v3_stream_part.py] Unused import: `LanguageModelV3ResponseMetadata` is imported (line 15) but never referenced — the intersection type was correctly flattened by inlining its fields, leaving the import dead. `ruff check` would flag this as F401.
[python/packages/provider/language_model/v3/language_model_v3_stream_part.py] The `finish_reason` field on `LanguageModelV3Finish` is typed `LanguageModelV3FinishReason | None = None`, but the TS source has `finishReason: LanguageModelV3FinishReason` as required (non-optional). This weakens the type contract, allowing `None` where the protocol never intends it.
[python/packages/provider/language_model/v3/language_model_v3_stream_part.py] The `timestamp` field on `LanguageModelV3ResponseMetadataPart` uses `Any | None` instead of `datetime | None`; the TS source types this as `Date`, which maps to Python's `datetime.datetime`.
[python/packages/provider/language_model/v3/language_model_v3_stream_part.py] Module docstring (lines 3–6) appears after `from __future__ import annotations` (line 1); PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/language_model/v2/language_model_v2_call_options.py] Within-package imports use absolute `ai_sdk.provider.language_model.v2.*` paths rather than relative imports (e.g., `from .language_model_v2_function_tool import ...`). The TypeScript source uses relative imports and the translation rules specify these should map to Python relative imports. This works when the package is installed but deviates from the stated rules.
[python/packages/provider/language_model/v2/language_model_v2_call_options.py] Module docstring (lines 3–6) appears after `from __future__ import annotations` (line 1). PEP 257 recommends the module docstring be the first statement in the file.
[python/packages/provider/language_model/v2/language_model_v2_call_options.py] TextResponseFormat and JSONResponseFormat are added as named public module-level symbols where TypeScript only had anonymous inline types inside the responseFormat union. This is necessary for Python but slightly expands the public API surface beyond what the TypeScript source exports.
[python/packages/provider/image_model/v2/image_model_v2_call_warning.py] ImageModelV2CallWarningUnsupportedSetting.setting is required in the TypeScript source (no `?`) but is given a default of `'prompt'` in the Python dataclass, making it effectively optional. The root cause is the `type` field having a default before `setting`, which Python positional dataclass rules require to also have a default. Fix: use `@dataclass(frozen=True, kw_only=True)` so `type` can keep its default while `setting` remains required (no default).
[python/packages/provider/image_model/v2/image_model_v2_call_warning.py] ImageModelV2CallWarningOther.message is required in the TypeScript source (no `?`) but is given a default of `''` in the Python dataclass, making it effectively optional. Same fix applies: use `kw_only=True` so `type` can keep its default while `message` stays required.
[python/packages/provider/image_model/v2/image_model_v2_call_warning.py] Module docstring appears after `from __future__ import annotations` (lines 3–6). PEP 257 recommends the module docstring be the very first statement in the file, before any imports including `__future__`.
[python/packages/provider/video_model/v3/video_model_v3_call_options.py] Template literal types `${number}:${number}` (aspectRatio) and `${number}x${number}` (resolution) are translated to plain `str` with no runtime validators. Translation rules §2 recommend adding a validator function called at construction time (e.g. in __post_init__).
[python/packages/provider/video_model/v3/video_model_v3_call_options.py] Relative TypeScript imports (`../../shared`, `./video-model-v3-file`) are translated to absolute Python imports (`from ai_sdk.provider.shared import ...`, `from ai_sdk.provider.video_model.v3.video_model_v3_file import ...`) rather than relative (`from ...shared import ...`, `from .video_model_v3_file import ...`). Both work, but translation rules §8 recommend preserving relative imports as relative.
[python/packages/provider/video_model/v3/video_model_v3_call_options.py] `from typing import Any` is imported but not used anywhere in the file. `SharedV3ProviderOptions` and `VideoModelV3File` handle their own typing internally.
[python/packages/provider/video_model/v3/video_model_v3_call_options.py] The field `n: int = 1` gives a Python default to what was a required (non-optional) field in the TypeScript type (`n: number`, no `?` or default). This relaxes construction constraints — callers can now omit `n` entirely. The comment says 'Default: 1' so this is defensible, but worth flagging.
[python/packages/provider/video_model/v3/video_model_v3_call_options.py] Module docstring appears after `from __future__ import annotations` (lines 3–6). PEP 257 recommends the module docstring be the very first statement in the file, before any imports.
[python/packages/provider/image_model/v2/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3 vs line 1). PEP 257 recommends the module docstring be the very first statement. However, the project's own translation rules explicitly show this same ordering, so it is intentional and consistent across the codebase.
[python/packages/provider/image_model/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 1 vs lines 3-6). PEP 257 specifies the module docstring should be the first statement of the module; PEP 236 permits __future__ imports only after the docstring (and comments). The correct order is: docstring first, then __future__ imports. This does not affect runtime behaviour but is technically incorrect per spec.
[python/packages/provider/video_model/v3/__init__.py] Module docstring appears after `from __future__ import annotations` (lines 1–2). Per PEP 257 and the Python language spec, the module docstring should be the first statement in the file; `from __future__` imports are explicitly allowed to follow a module docstring, so the correct order is: docstring first, then `from __future__ import annotations`.
[python/packages/provider/transcription_model/__init__.py] Module docstring appears after 'from __future__ import annotations' on line 1. PEP 257 recommends the docstring be the first statement in the module; placing it after the future import is a common but debatable convention. Same pattern is present in the v2 and v3 sub-package __init__.py files.
[python/packages/provider/reranking_model/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the module docstring be the very first statement in the file, before any imports including __future__ imports.
[python/packages/provider/speech_model/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3 vs line 1). PEP 257 recommends the module docstring be the first statement, but 'from __future__' imports must precede all other code, making this placement a common accepted trade-off.
[python/packages/provider/language_model/v3/language_model_v3_call_options.py] Module docstring is placed after 'from __future__ import annotations' (lines 3–6). PEP 257 recommends the module docstring be the very first statement, but since 'from __future__ import annotations' is a language requirement that must appear first, this ordering is widely accepted in practice. Cosmetic only.
[python/packages/provider/language_model/v3/language_model_v3_call_options.py] ResponseFormatText and ResponseFormatJson are not explicitly named in the TypeScript source (they are inline anonymous types within the responseFormat union). Elevating them to named, exported dataclasses is the correct Python approach, but they are not present in the TS public surface — if the package's __init__.py re-exports LanguageModelV3CallOptions, it may also need to re-export ResponseFormatText, ResponseFormatJson, and ResponseFormat so callers can construct/type-check response_format values without importing from an internal module.
[python/packages/provider/language_model/v2/language_model_v2_call_options.py] Intra-package imports use absolute paths (e.g. `from ai_sdk.provider.language_model.v2.language_model_v2_function_tool import ...`) rather than relative paths (e.g. `from .language_model_v2_function_tool import ...`). The translation rules explicitly map relative TS imports to relative Python imports. These will work at runtime if the package is installed as `ai_sdk.provider`, but deviate from the stated convention and could break in editable-install or path-only setups.
[python/packages/provider/language_model/v2/language_model_v2_call_options.py] Module docstring appears after `from __future__ import annotations` rather than as the very first statement in the file. PEP 257 recommends the module docstring be the first statement; `from __future__ import annotations` is a special case but some linters will flag this ordering.
[python/packages/provider/embedding_model/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' rather than as the first statement of the file. PEP 257 recommends the module docstring be the first statement, but this placement is consistent with the project-wide translation convention of always putting 'from __future__ import annotations' first.
[python/packages/provider/embedding_model/v3/__init__.py] EmbeddingModelV3ResultResponse and EmbeddingModelV3ResultUsage are re-exported from __init__.py even though the TypeScript source only has a single export (EmbeddingModelV3Result) from that module — the inline anonymous types were not separately exported in TS. Re-exporting Python-extracted helper types through the barrel is a reasonable design choice that expands the public API surface slightly beyond the TS original.
[python/packages/provider/language_model/v3/language_model_v3_generate_result.py] Module docstring appears after `from __future__ import annotations` (lines 1 and 3–6). PEP 257 recommends the docstring be the very first statement in the module; `from __future__ import annotations` should follow it. Currently this is inverted.
[python/packages/provider/language_model/v3/language_model_v3_generate_result.py] The required TypeScript fields `content`, `finishReason`, `usage`, and `warnings` are all given Python defaults (making them optional at construction). This relaxes the original contract. It is a practical dataclass necessity given the intervening optional fields, but worth noting in case a stricter approach (e.g. TypedDict or a separate builder) is preferred.
[python/packages/provider/language_model/v3/language_model_v3_generate_result.py] LanguageModelV3ResponseMetadata is not imported; instead its three fields (id, timestamp, model_id) are manually inlined into LanguageModelV3GenerateResultResponse. The inlined fields match the translated LanguageModelV3ResponseMetadata dataclass exactly, so this is functionally correct — but the approach (flattening vs. inheriting) could be made explicit with a comment referencing the source intersection type.
[python/packages/provider/video_model/v3/video_model_v3.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 states the module docstring must be the very first statement in the file; `from __future__ import annotations` should follow it.
[python/packages/provider/video_model/v3/video_model_v3.py] Within-package imports use absolute paths (`ai_sdk.provider.shared.v3.*`, `ai_sdk.provider.video_model.v3.*`) rather than relative imports (`.shared.v3.*`, `.video_model.v3.*`). The translation rules map `./foo` → `from .foo import ...`, suggesting relative imports for intra-package references.
[python/packages/provider/language_model/v3/language_model_v3_stream_result.py] The `stream` field is given a `default_factory=lambda: _empty_async_iterator()`, making it optional at construction time. In the TypeScript source, `stream` is a required field (no `?`). A more faithful translation would declare `stream` without a default (e.g., `stream: AsyncIterator[LanguageModelV3StreamPart]`) and let the two optional fields follow it, which is valid Python dataclass syntax.
[python/packages/provider/language_model/v3/language_model_v3_stream_result.py] The `# noqa: RET504` comment references the wrong ruff rule. `RET504` means 'unnecessary assignment to variable before return statement'. The warning actually being suppressed is the unreachable `yield` after `return` in the async generator (likely `B901` or a similar control-flow rule). The code functions correctly, but the noqa suppression is mislabelled.
[python/packages/provider/language_model/v3/language_model_v3_stream_result.py] Module docstring appears after `from __future__ import annotations` (line 2) rather than as the very first statement. PEP 257 recommends the module docstring be the first string literal in the file; `from __future__ import annotations` is conventionally the only allowed predecessor.
[python/packages/provider/image_model/v2/image_model_v2.py] Unused import: `JSONArray` is imported from `ai_sdk.provider.json_value` (line 18) but is never referenced in the file body. `ImageModelV2ProviderMetadata` was simplified to `dict[str, JSONValue]`, leaving `JSONArray` dead. This will trigger ruff F401. The import should be removed.
[python/packages/provider/image_model/v2/image_model_v2.py] Type precision loss on `ImageModelV2ProviderMetadata`: the TypeScript source is `Record<string, { images: JSONArray } & JSONValue>`, which requires each value to have an `images: JSONArray` key. The Python renders it as `dict[str, JSONValue]`, dropping that structural constraint. Per the intersection-type translation rules this is an accepted simplification, but a `TypedDict` subclass (e.g. `class ProviderImageMeta(TypedDict): images: JSONArray`) would be more faithful if higher fidelity is desired.
[python/packages/provider/embedding_model_middleware/v3/embedding_model_v3_middleware.py] override_provider and override_model_id are annotated to return `str | None`, but the TypeScript originals return `string` (non-nullable) — only the property itself is optional, not its return value. This makes callers unnecessarily guard against `None` on a value that, when the method exists, is always a string. Consider changing to `-> str`.
[python/packages/provider/image_model/v3/image_model_v3.py] Unused imports: `JSONArray` and `JSONValue` are imported from `ai_sdk.provider.json_value` but are never referenced — `ImageModelV3ProviderMetadata` is typed as `dict[str, Any]` instead of using those types. These will trigger `ruff F401` on `uv run ruff check`.
[python/packages/provider/image_model/v3/image_model_v3.py] The `response` field in `ImageModelV3GenerateResponse` is given a `default_factory` (making it optional at construction time), but in the TypeScript source `response` is a required field (no `?`). A provider implementing `do_generate` could accidentally omit it and silently receive a default with `datetime.now()` and empty `model_id`, leading to bad telemetry data. Consider removing the default to enforce the TS contract.
[python/packages/provider/image_model/v3/image_model_v3.py] GetMaxImagesPerCallFunction uses `Callable[..., int | None | Awaitable[int | None]]` with ellipsis for arguments, losing the TypeScript specificity that the callable receives `{ modelId: string }`. A typed `Protocol` or a `Callable[[ModelIdOptions], ...]` with a `TypedDict` argument would preserve the contract more faithfully.
[python/packages/provider/image_model/v3/image_model_v3.py] Module docstring appears as the second statement, after `from __future__ import annotations`. PEP 257 recommends the module docstring be the first statement of the module. While this is a valid pattern commonly seen in practice, it is a minor PEP 257 deviation.
[python/packages/provider/image_model/__init__.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 recommends the module docstring be the first statement in the file, with __future__ imports following it. This is a minor style point and does not affect runtime behaviour.
[python/packages/provider/video_model/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 2). PEP 257 recommends the module docstring be the very first statement in the file; 'from __future__ import annotations' should follow it.
[python/packages/provider/embedding_model/__init__.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6). PEP 257 recommends the module docstring be the first statement of the module. Consider moving it before the __future__ import.
[python/packages/provider/language_model/v3/language_model_v3_generate_result.py] Mixed import style: same-package cross-directory imports use absolute paths (e.g. `from ai_sdk.provider.shared.v3.shared_v3_headers import SharedV3Headers`) while same-directory imports use relative paths (e.g. `from .language_model_v3_content import ...`). Relative imports (e.g. `from ..shared.v3.shared_v3_headers import SharedV3Headers`) would be more consistent and robust.
[python/packages/provider/language_model/v3/language_model_v3_generate_result.py] Module docstring appears on line 3, after `from __future__ import annotations` on line 1. PEP 257 recommends the module docstring be the first statement, but since `from __future__` must precede all other code, this placement is the only valid option — worth noting for consistency across translated files.
[python/packages/provider/language_model/v2/language_model_v2_call_warning.py] Module docstring appears after `from __future__ import annotations` (line 3). PEP 257 recommends the module docstring be the very first statement in the file, before any imports.
[python/packages/provider/language_model/v2/language_model_v2_call_warning.py] Same-directory relative TS imports (`from './language-model-v2-function-tool'`, `from './language-model-v2-provider-defined-tool'`) were translated as absolute `ai_sdk.provider.language_model.v2.*` imports rather than relative Python imports (`from .language_model_v2_function_tool import ...`). Translation rules specify that relative TS imports map to relative Python imports.
[python/packages/provider/language_model/v2/language_model_v2_call_warning.py] `UnsupportedSettingWarning.setting` (required in TS) is given a default of `'temperature'`, and `UnsupportedToolWarning.tool` (required in TS) is given `= None` with `# type: ignore[assignment]`, both as workarounds for Python dataclass field-ordering rules. This allows constructing instances with missing required fields at runtime. Consider using `dataclasses.field` with a sentinel or `__post_init__` validation to enforce the required-field constraint.
[python/packages/provider/embedding_model_middleware/v3/embedding_model_v3_middleware.py] All callback fields flatten the TypeScript options-object pattern into positional-argument Callable types (e.g. TS `(options: { model: EmbeddingModelV3 }) => string` → Python `Callable[[EmbeddingModelV3], str]`). This is a reasonable adaptation given Python's Callable limitations, but it changes the calling convention for external implementors. A more faithful alternative would define a small TypedDict or dataclass for each options shape.
[python/packages/provider/embedding_model_middleware/v3/embedding_model_v3_middleware.py] Module docstring is placed after `from __future__ import annotations` rather than as the file's very first statement. PEP 257 recommends the module docstring be first, but `from __future__` imports must technically precede everything else — a known structural tension shared across many files in this project.
[python/packages/provider/image_model_middleware/v3/image_model_v3_middleware.py] Optional TS fields (marked with `?`) were translated to mandatory Protocol methods with `| None` return types. In TypeScript, `overrideProvider?` means the field may be absent; if present it returns `string` (never null). In Python, `override_provider` is now a required Protocol method returning `str | None`, shifting optionality from field-presence to return-value. This means consuming code cannot safely `await middleware.transform_params(...)` or `await middleware.wrap_generate(...)` without a `None` guard first, and must treat `override_provider`/`override_model_id` returns as nullable even though the TS contract guarantees a non-null `string` when those methods exist. A cleaner alternative would be to omit optional methods from the Protocol and rely on `hasattr` checks in the application layer.
[python/packages/provider/image_model_middleware/v3/image_model_v3_middleware.py] Duplicate `| None` in `override_max_images_per_call` return type: `int | None | GetMaxImagesPerCallFunction | None` — the trailing `| None` is redundant since `None` is already in the union. Python normalises this at runtime but it is misleading to read.
[python/packages/provider/image_model_middleware/v3/image_model_v3_middleware.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 recommends the docstring be the first statement of the module; however, PEP 236 requires `from __future__` imports to precede all other statements, so this ordering is the accepted convention. Low-priority style note only.
[python/packages/provider/video_model/v3/video_model_v3.py] Relative TS imports (`'./video-model-v3-call-options'`, `'../../shared/v3/shared-v3-provider-metadata'`, `'../../shared/v3/shared-v3-warning'`) were translated as absolute `ai_sdk.provider.*` imports instead of relative Python imports (`.video_model_v3_call_options`, `...shared.v3.shared_v3_provider_metadata`, etc.) as specified by the translation rules. This is consistent with the project-wide convention used in companion files (e.g. `video_model_v3_call_options.py` does the same), but if the package is not installed/configured as `ai_sdk.provider`, these imports will fail at runtime. Recommend verifying that `ai_sdk.provider` is a resolvable package path in the Python environment.
[python/packages/provider/video_model/v3/video_model_v3.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 recommends the module docstring be the very first statement of the file, before any import. This is a minor style note — the same pattern appears throughout the project, so it may be an intentional project-wide convention.
[python/packages/provider/language_model/v3/language_model_v3.py] Module docstring placement: `from __future__ import annotations` (line 1) appears before the module docstring (lines 3–6). Per PEP 257 and PEP 236, the module docstring should be the very first statement, with `from __future__` immediately following it. As currently written, the triple-quoted string is not recognised as a module docstring by pydoc or other introspection tools.
[python/packages/provider/embedding_model_middleware/v3/__init__.py] Unused import: `field` is imported from `dataclasses` but never used in the class body (all field defaults are bare literals or `None`, not `field(...)`). Ruff/pyright will flag this.
[python/packages/provider/embedding_model_middleware/v3/__init__.py] TypeScript callbacks take named option-bag objects (e.g. `(options: { model: EmbeddingModelV3 }) => string`) but were translated to positional `Callable` signatures (e.g. `Callable[[EmbeddingModelV3], str]`). This is a defensible Python simplification, but changes the calling convention that middleware implementors must follow — if any downstream caller passes a dict/dataclass instead of positional args, it will fail. Consider using `Protocol` classes for each callback type to make the signature explicit.
[python/packages/provider/language_model/v2/language_model_v2_stream_part.py] timestamp field in LanguageModelV2ResponseMetadataPart is typed as `Any | None` instead of `datetime | None`; TypeScript `Date` maps to Python `datetime.datetime`, not `Any`. No runtime impact, but loses static type safety.
[python/packages/provider/language_model/v2/language_model_v2_stream_part.py] finish_reason in LanguageModelV2Finish is `LanguageModelV2FinishReason | None = None` whereas the TypeScript source has `finishReason: LanguageModelV2FinishReason` as a required field. This is forced by Python dataclass field-ordering constraints (all fields after the defaulted `type` literal must have defaults), but a note is warranted.
[python/packages/provider/language_model/v2/language_model_v2_stream_part.py] Module docstring appears after `from __future__ import annotations` (lines 3-6). PEP 257 recommends the module docstring be the very first statement in the file, before any imports including __future__.
[python/packages/provider/language_model/v3/__init__.py] Module docstring appears on line 2, after `from __future__ import annotations` on line 1. PEP 257 recommends the docstring be the first statement in the module. This is a common tension with `from __future__` imports (which must come first for correctness) and does not affect runtime behaviour, but static analysers like pylint may flag it.
[python/packages/provider/provider/v3/provider_v3.py] Module docstring appears after `from __future__ import annotations` (line 3); PEP 257 recommends the module docstring be the first statement of the file.
[python/packages/provider/provider/v3/provider_v3.py] TypeScript marks four methods as optional (`textEmbeddingModel?`, `transcriptionModel?`, `speechModel?`, `rerankingModel?`), meaning implementors may omit them. Python `Protocol` has no optional-method concept — all declared methods become structurally required, so `isinstance(obj, ProviderV3)` will reject objects that omit these methods unlike TypeScript. This is a Python type-system limitation with no clean workaround; worth noting in package documentation.
[python/packages/provider/image_model_middleware/__init__.py] Optional TypeScript interface members (overrideProvider?, overrideModelId?, overrideMaxImagesPerCall?, transformParams?, wrapGenerate?) are translated as required Protocol methods with '| None' return types. Python Protocols require all declared methods to be implemented, so a class omitting any of these would fail structural type checks — unlike TypeScript where optional members may be absent. Consider using ABC with default 'return None' implementations to preserve the optional semantics.
[python/packages/provider/image_model_middleware/__init__.py] override_max_images_per_call return type is 'int | None | GetMaxImagesPerCallFunction | None' — the trailing '| None' is redundant since None is already in the union. Should be 'int | None | GetMaxImagesPerCallFunction'.
[python/packages/provider/image_model_middleware/__init__.py] Module docstrings in all three files (image_model_middleware/__init__.py, v3/__init__.py, v3/image_model_v3_middleware.py) appear after 'from __future__ import annotations'. PEP 257 recommends the module docstring be the very first statement in the file.
[python/packages/provider/embedding_model_middleware/__init__.py] embedding_model_v3_middleware.py imports `field` from `dataclasses` but never uses it — all field defaults are `None` or a literal. Consider removing the unused import.
[python/packages/provider/embedding_model_middleware/__init__.py] Both __init__.py files place the module docstring after `from __future__ import annotations`. PEP 257 recommends the module docstring be the first statement in the file, with `from __future__ import annotations` following it.
[python/packages/provider/language_model_middleware/v3/language_model_v3_middleware.py] Module docstring appears after 'from __future__ import annotations' (lines 1 then 3-9). PEP 257 recommends the module docstring be the first statement in the file. Consider swapping their order.
[python/packages/provider/language_model_middleware/v3/language_model_v3_middleware.py] All TS callbacks received named options objects (e.g., 'overrideProvider: (options: { model: LanguageModelV3 }) => string'), but the Python translation flattens each to positional Callable arguments (e.g., 'Callable[[LanguageModelV3], str]'). This is idiomatic Python and internally consistent across the file, but callers of these middleware hooks elsewhere in the codebase must use the same positional convention — worth verifying when the middleware application layer is translated.
[python/packages/provider/language_model/v2/language_model_v2.py] LanguageModelV2ResponseMetadata is imported (line 19) but never directly referenced anywhere in the file — its fields were inlined into LanguageModelV2GenerateResponse rather than used via composition. Ruff would flag this as F401 (imported but unused). Consider either removing the import or adding a comment explaining it is intentionally inlined.
[python/packages/provider/language_model/v2/language_model_v2.py] LanguageModelV2GenerateResponse.timestamp uses Any instead of datetime | None. TypeScript Date maps to Python datetime (from the datetime stdlib module), not Any. No runtime failure, but type checkers (pyright/mypy) lose precision here.
[python/packages/provider/language_model/v2/language_model_v2.py] Module docstring (lines 3-6) appears after the from __future__ import annotations statement (line 1). PEP 257 recommends the module docstring be the very first statement in the file, before any imports including __future__ imports.
[python/packages/provider/language_model/v2/language_model_v2.py] import re (line 8) is used only inside type annotations. Because from __future__ import annotations is active, all annotations are stringified and re is never evaluated at runtime. It could be moved under a TYPE_CHECKING guard, though leaving it as an unconditional import is not a runtime error.
[python/packages/provider/provider/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the module docstring be the very first statement in the file. Consider moving the docstring above the __future__ import, or accepting this as a project-wide convention.
[python/packages/provider/language_model/v2/__init__.py] Module docstring (lines 2-5) appears after 'from __future__ import annotations' (line 1) rather than as the very first statement. PEP 257 recommends the module docstring precede all other statements, including __future__ imports. This is a style-only concern; the file is functionally correct.
[python/packages/provider/language_model_middleware/v3/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3). PEP 257 recommends the docstring be the very first statement in the file. Many projects treat 'from __future__ import annotations' as an exception, so this is not a hard failure, but worth noting for consistency.
[python/packages/provider/provider/v2/provider_v2.py] Module docstring is placed after `from __future__ import annotations`, so Python will not assign it to `module.__doc__` (the first statement is the import, not the string literal). PEP 257 recommends the docstring come first; PEP 236 explicitly permits module docstrings before future-import statements. Correct order: module docstring → `from __future__ import annotations` → other imports.
[python/packages/provider/language_model_middleware/v2/language_model_v2_middleware.py] Callable signatures flatten TypeScript options-object parameters into positional parameters (e.g., `(options: { model: LanguageModelV2 }) => string` becomes `Callable[[LanguageModelV2], str]`). This is a consistent design decision but the middleware runner code that *calls* these hooks must match this positional convention.
[python/packages/provider/language_model_middleware/v2/language_model_v2_middleware.py] Several field docstrings retain 'promise' language from the TypeScript source (e.g., 'Returns a promise that resolves to the transformed parameters.'). The Python idiom would say 'Returns an awaitable / coroutine that resolves to…'.
[python/packages/provider/language_model/__init__.py] Module docstring appears after 'from __future__ import annotations'; Python will not assign it to module.__doc__ in this position. However, this ordering matches the pattern shown in the project's own translation rules, so it is consistent with project convention.
[python/packages/provider/language_model_middleware/v2/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 1). PEP 257 recommends the module docstring be the first statement in the file, before any imports including __future__ imports.
[python/packages/provider/provider/v2/provider_v2.py] Module docstring is placed after `from __future__ import annotations` (lines 1–6). PEP 257 states the module docstring should be the first statement, but `from __future__` must syntactically precede everything else. The current ordering is the accepted practical convention and consistent with the rest of the codebase — worth noting for style-guide documentation but not a code defect.
[python/packages/provider/provider/v2/provider_v2.py] @runtime_checkable was added to ProviderV2(Protocol) even though the TypeScript source is a plain interface with no explicit runtime-check requirement. This is a reasonable Python affordance enabling isinstance() checks, but annotators may want a policy decision on whether all Protocol translations should include it.
[python/packages/provider/provider/v2/provider_v2.py] The text_embedding_model docstring 'Returns' section reads 'The language model associated with the id' — this faithfully preserves a copy-paste inconsistency present in the TypeScript source (which also wrote '@returns {LanguageModel}' for textEmbeddingModel). Not a translation error, but worth fixing in a future pass.
[python/packages/provider/provider/v2/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3) rather than before it (line 1). PEP 257 recommends the module docstring be the first statement in the file; the __future__ import is an exception but many style guides still place the docstring first.
[python/packages/provider/language_model_middleware/__init__.py] Module docstring appears after 'from __future__ import annotations' (line 3 vs line 1). PEP 257 says a module docstring should be the first statement, but 'from __future__' imports must precede everything else — this ordering is accepted practice and pyright/ruff do not flag it, but it is worth noting for project-wide consistency.
[python/packages/provider/provider/__init__.py] Module docstring appears after 'from __future__ import annotations' (lines 3-6); PEP 257 recommends the module docstring be the first statement in the file.
[python/packages/provider/__init__.py] __all__ does not explicitly enumerate the exports from language_model: the file uses `from .language_model import *` which brings those names into the module namespace, but the `__all__` list only contains a comment placeholder (`# language_model (re-exported via wildcard)`) rather than the actual names. This means `from provider import *` will silently omit all language_model exports, diverging from the TypeScript `export * from './language-model/index'` behaviour. Consider enumerating the language_model names explicitly in __all__, or dynamically extending __all__ with the language_model subpackage's own __all__.
[python/packages/provider/__init__.py] Module docstring appears after `from __future__ import annotations` (lines 3-6 vs line 1). Python's own docs (PEP 236) note that a module docstring is one of the few things allowed to precede a __future__ import. Placing the docstring first (`"""..."""` then `from __future__ import annotations`) is the PEP 257-compliant order and is accepted by all Python versions.
